// createContext: It allows us to know what the information we are going to put there looks like and also to define the provider. 
import { createContext } from "react";

export const UserContext = createContext()